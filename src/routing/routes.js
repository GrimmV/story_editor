
export const toStoryOverview = "/stories";
export const toStoryEditor = (storyId) => toStoryOverview + "/" + storyId;
export const toStoryEditorFrame = (storyId, frameId) => toStoryOverview + "/" + storyId + "/" + frameId;